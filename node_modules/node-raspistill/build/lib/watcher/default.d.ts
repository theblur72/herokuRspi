/// <reference types="node" />
import { IWatcher, IWatcherOptions } from './interfaces';
export declare class DefaultWatcher implements IWatcher {
    protected _options: IWatcherOptions;
    private readonly _imageInProgressSymbol;
    private readonly _forceCloseEvent;
    private readonly _defaultOptions;
    private _watcher;
    private _messages;
    constructor(options?: IWatcherOptions);
    setOptions(options: IWatcherOptions): void;
    getOption(key: string): any;
    getOptions(): IWatcherOptions;
    watchAndGetFile(filePath: string, options?: IWatcherOptions): Promise<Buffer>;
    watchAndGetFiles(dirPath: string, watchTimeMs: number, cb: (file: Buffer) => any): Promise<void>;
    closeWatcher(): void;
    private _makeDir;
    private addForceCloseHandler;
}
