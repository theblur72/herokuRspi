import { IClaMapper } from './cla-mapper.interface';
export declare class ClaMapper implements IClaMapper {
    get commandLineArgsMap(): Record<string, string>;
    set commandLineArgsMap(val: Record<string, string>);
    private _commandLineArgsMap;
    constructor(commandLineArgsMap: Record<string, string>);
    getCommandLineArgs(obj: any): string[];
}
