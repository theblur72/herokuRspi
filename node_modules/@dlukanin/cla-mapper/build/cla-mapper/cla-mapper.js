"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ClaMapper = void 0;
class ClaMapper {
    constructor(commandLineArgsMap) {
        this._commandLineArgsMap = commandLineArgsMap;
    }
    get commandLineArgsMap() {
        return this._commandLineArgsMap;
    }
    set commandLineArgsMap(val) {
        this._commandLineArgsMap = val;
    }
    getCommandLineArgs(obj) {
        const args = [];
        Object.entries(obj).forEach((entry) => {
            const key = entry[0];
            const value = entry[1];
            const arg = this._commandLineArgsMap[key];
            if (arg) {
                const type = typeof value;
                if (type === 'boolean') {
                    if (value) {
                        args.push(arg);
                    }
                }
                else if (type === 'string' || type === 'number') {
                    args.push(arg);
                    args.push(value.toString());
                }
            }
        });
        return args;
    }
}
exports.ClaMapper = ClaMapper;
//# sourceMappingURL=cla-mapper.js.map